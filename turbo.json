{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["API_URL"],
      "outputs": ["dist/**"]
    },
    "web#build": {
      "dependsOn": ["^build"],
      "env": ["API_URL"],
      "outputs": [".next/**"]
    }
  },
  "globalDependencies": ["tsconfig.json", "**/.env.*local", ".env"]
}
