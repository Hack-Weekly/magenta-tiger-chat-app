import { Button, Input } from "ui/components";

export default function Web() {
  return (
    <div>
      <h1>Web</h1>
      <Button size="large" text="Buttontext" />
      <Input type={"search"} />
    </div>
  );
}
